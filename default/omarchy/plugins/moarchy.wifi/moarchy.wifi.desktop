[Desktop Entry]
Type=Application
Name=Wi-Fi
GenericName=Network
Comment=Join a wireless network
# A shell plugin is not a process you launch, it is a screen the shell already
# holds. `toggle` and not `summon` so a second tap on the drawer icon puts it
# away again, which is what every other entry here does.
Exec=omarchy-shell shell toggle moarchy.wifi
# An absolute path, not a themed name. AppLibrary.iconSource() returns a file
# URL for anything starting with "/" and never consults the icon theme, which
# is the point: the themed name resolved to a dark monochrome glyph on the
# drawer's dark sheet, so the entry was drawn and unreadable. The file ships
# beside the plugin it belongs to, at the path the package installs it to.
Icon=/usr/share/moarchy/plugins/moarchy.wifi/icon.svg
Terminal=false
Categories=Settings;Network;
Keywords=wifi;wlan;wireless;network;internet;passphrase;
# Ownership marker: the packaging moves entries out of the plugin directory and
# names the plugin they belong to.
X-Moarchy-Plugin=moarchy.wifi
